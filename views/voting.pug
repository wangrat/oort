extends layout

block content
  doctype html
  html(lang="en")
    head
      title= "Spotify Voting"
    body
      h1 Spotify Voting Page!
      #container.col
        h1 Room ID = #{roomID}

      each element in songLists
        p #{element.name}

    img.className#IdName(src="./res/clippy.gif" alt="Image Title")
